extends "res://scripts/stash_menu_system_v42_safe.gd"

# v0.43 stable Shared Stash wrapper.
# Item rows continue using the shared ItemIconRegistry cache; no per-item
# decode, preload, or base64 parsing is performed by this menu.
